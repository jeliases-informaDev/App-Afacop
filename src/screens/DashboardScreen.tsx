import React,{useCallback,useEffect,useRef,useState}from'react';import{RefreshControl,StyleSheet,Text,View}from'react-native';import{LinearGradient}from'expo-linear-gradient';import{MaterialCommunityIcons}from'@expo/vector-icons';import{useAuth}from'../AuthContext';import{Badge,Card,Empty,Header,Loading,Screen}from'../ui';import{C}from'../theme';
export default function DashboardScreen({refreshRevision=0}:{refreshRevision?:number}){const{api,user}=useAuth();const[data,setData]=useState<any>(null);const[loading,setLoading]=useState(true);const[refreshing,setRefreshing]=useState(false);const[error,setError]=useState('');const mounted=useRef(true);const requestId=useRef(0);const load=useCallback(async(background=false)=>{const current=++requestId.current;if(background)setRefreshing(true);try{const r:any=await api('/api/campo/resumen');if(!mounted.current||current!==requestId.current)return;setData(r.data);setError('')}catch(e:any){if(mounted.current&&current===requestId.current)setError(e.message)}finally{if(mounted.current&&current===requestId.current){setLoading(false);setRefreshing(false)}}},[api]);useEffect(()=>{mounted.current=true;load();const timer=setInterval(()=>load(true),20000);return()=>{mounted.current=false;clearInterval(timer)}},[load]);useEffect(()=>{if(refreshRevision>0)load(true)},[refreshRevision,load]);if(loading&&!data)return<Loading/>;return<Screen refreshControl={<RefreshControl refreshing={refreshing} onRefresh={()=>load(true)} colors={[C.primary]}/>}><Header title="Mi jornada" subtitle="Gestión de campo · actualización en tiempo real"/><LinearGradient colors={[C.navy,C.primary]} style={s.hero}><Text style={s.hello}>HOLA, {(user?.nombres||user?.username||'ASESOR').toUpperCase()}</Text><Text style={s.title}>Tu operación de hoy,{String.fromCharCode(10)}en un solo lugar.</Text><View style={s.sync}><View style={s.dot}/><Text style={s.syncText}>{refreshing?'Actualizando datos…':'Sincronizado con Mi Radar 360° Web'}</Text></View></LinearGradient>{error&&!data?<Empty title="No pudimos cargar tu jornada" text={error}/>:<><View style={s.grid}>{[['Clientes en ruta',data?.clientes_ruta||0,'account-multiple'],['Pendientes',data?.pendientes||0,'clock-outline'],['Gestionados',data?.gestionados||0,'check-decagram'],['Cartera asignada',data?.clientes_asignados||0,'briefcase-account']].map(([label,value,icon])=><Card key={String(label)} style={s.kpi}><MaterialCommunityIcons name={icon as any} size={23} color={C.primary}/><Text style={s.value}>{value}</Text><Text style={s.label}>{label}</Text></Card>)}</View><Card><View style={s.routeRow}><View style={s.routeIcon}><MaterialCommunityIcons name="map-marker-path" size={26} color={C.primary}/></View><View style={{flex:1}}><Text style={s.routeTitle}>{data?.ruta?'Ruta #'+data.ruta.id_ruta:'Sin ruta para hoy'}</Text><Text style={s.routeSub}>{data?.ruta?'Revisa el orden de visitas en Mi ruta.':'Tu supervisor debe programar una ruta.'}</Text></View>{data?.ruta?<Badge status={data.ruta.estado}/>:null}</View></Card></>}</Screen>}
const s=StyleSheet.create({hero:{padding:22,borderRadius:24,gap:10,overflow:'hidden'},hello:{color:'#8DB1FF',fontWeight:'900',fontSize:10,letterSpacing:1.2},title:{color:'#fff',fontSize:27,lineHeight:32,fontWeight:'900'},sync:{flexDirection:'row',alignItems:'center',gap:7,marginTop:4},dot:{width:7,height:7,borderRadius:7,backgroundColor:'#4CE0A7'},syncText:{color:'#C6D5F6',fontSize:11},grid:{flexDirection:'row',flexWrap:'wrap',gap:10},kpi:{width:'48%',minHeight:128},value:{fontSize:28,fontWeight:'900',color:C.text,marginTop:10},label:{fontSize:11,fontWeight:'700',color:C.muted,marginTop:3},routeRow:{flexDirection:'row',alignItems:'center',gap:11},routeIcon:{width:48,height:48,borderRadius:15,backgroundColor:'#EEF2FF',alignItems:'center',justifyContent:'center'},routeTitle:{fontSize:16,fontWeight:'900',color:C.text},routeSub:{fontSize:11,lineHeight:16,color:C.muted,marginTop:3}});
